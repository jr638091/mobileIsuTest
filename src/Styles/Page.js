import { StyleSheet } from 'react-native';

const PageStyles = StyleSheet.create({
  fullPage: {
    height: '100%',
    backgroundColor: '#fff',
    padding: '5%',
  },
  horizontal: {
    flexDirection: 'row',
  },
});

export default PageStyles;
